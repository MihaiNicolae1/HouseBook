{% extends 'base.html.twig' %}

{% block title %}HouseBook | Profile{% endblock %}

{% block body %}
<div class="d-flex justify-content-center">
    <img class="img-fluid-3 img-thumbnail" src={{asset ('/uploads/profile_pictures/' ~ app.user.profilePicture) }}></img>
</div>

<div class="d-flex justify-content-center">
<h3>First Name: {{ app.user.firstName}}</h3>
</div>

<div class="d-flex justify-content-center">
<h3>Last Name: {{ app.user.lastName}}</h3>
</div>
{% endblock %}
